docker:
  username: zhoudian64
  tag: 0.0.1
cicd:
  GitHubAction: true
  k8s: true
database:
  dbengine: pgsql
  tablename: student
  fields:
    - name: name
      type: varchar(24)
    - name: birthday
      type: date
