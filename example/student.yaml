docker:
  username: zhoudian64
  tag: 0.0.1
GitHubAction: true
k8s:
  host: cloud.shuosc.com
  uri: /api/student
  namespace: development
database:
  dbengine: pgsql
  tablename: student
  url: postgres://shuosc@shu-course-postgres-svc:5432/shu-course?sslmode=disable
  fields:
    - name: name
      type: varchar(24)
    - name: birthday
      type: date
    - name: school_id
      type: int
