docker:
  username: zhoudian64
  tag: 0.0.1
GitHubAction: true
k8s:
  host: cloud.shuosc.com
  uri: /api/student
  namespace: development
database:
  dbengine: mysql
  tablename: student
  url: mysql://user:password@tcp(mysql:3306)/database
  fields:
    - name: name
      type: varchar(24)
    - name: birthday
      type: date
    - name: school_id
      type: int
